# EIE 官网主页重构方案

## 📋 设计概述

**设计风格**：专业物流行业风格，参考 GFS (Global Freight Solutions)
**核心配色**：蓝色系（主色调）+ 橙色（强调色）
**布局方式**：左文右图，信息层次清晰
**背景图片**：EIE 品牌货机起飞照片

---

## 🎨 视觉设计方案

### 1. Hero 区域布局

```
┌────────────────────────────────────────────────────────────────┐
│                    [Fixed Header - 半透明]                     │
│  [EIE Logo]  [Domestic ↓] [Customs ↓] [After-Sales ↓]        │
│              [About & Contact]              [Get a Quote 🟠]  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  [────────── 60% ──────────]  [──── 40% ────]                │
│                                                                │
│  ┌───────────────────────┐    ┌─────────────────┐            │
│  │ 左侧内容区             │    │  右侧视觉区      │            │
│  │                       │    │                 │            │
│  │ Global Direct         │    │  ┌───────────┐ │            │
│  │ Ecommerce Solution    │    │  │ 👤👤👤 4.8⭐│ │            │
│  │ by Postal Service     │    │  │ Review Card│ │            │
│  │                       │    │  └───────────┘ │            │
│  │ [副标题描述文字]       │    │                 │            │
│  │                       │    │  [EIE Cargo    │            │
│  │ [Get Started 🟠]      │    │   Plane Photo] │            │
│  │ [Track Package ⚪]    │    │                 │            │
│  │                       │    │  CN ═══► CA    │            │
│  │ 📦 UNLOADING          │    │  May 3 → May 9│            │
│  │ Book Container        │    │                 │            │
│  │ Unloading Today       │    │                 │            │
│  │ [Book Now →]          │    │                 │            │
│  └───────────────────────┘    └─────────────────┘            │
│                                                                │
│  [背景：深蓝渐变 + EIE货机照片（右侧） + 蓝色叠加层 60%]      │
└────────────────────────────────────────────────────────────────┘
```

---

## 🎯 核心设计元素详解

### Header 导航栏

**样式规范**：
```css
位置：Fixed，滚动时固定在顶部
背景：rgba(10, 38, 71, 0.95) - 半透明深蓝
高度：80px
内边距：0 80px

Logo：
- 位置：左上角
- 尺寸：高度 40px，宽度自适应
- 颜色：保持原有品牌色（蓝色渐变）

导航菜单：
- 字体：Inter Medium, 16px
- 颜色：#FFFFFF
- 间距：40px
- Hover：color: #FF9500 (橙色)
- 下拉箭头：↓ (Font Awesome)

CTA按钮 (Get a Quote)：
- 背景：#FF9500 (橙色)
- 文字：#FFFFFF, 16px, Inter SemiBold
- 圆角：8px
- 尺寸：160px × 48px
- 阴影：0 8px 16px -4px rgba(255, 149, 0, 0.3)
- Hover：background: #FFB340
```

---

### 左侧内容区 (60%)

#### 1. 主标题

```
文字内容：
Global Direct Ecommerce Solution
by Postal Service

样式规范：
- 字体：Inter Bold
- 字号：56px / 行高 64px
- 颜色：#FFFFFF
- 最大宽度：600px
- 间距：margin-bottom: 24px
- 动画：Fade In + Slide Up (0.8s, ease-out)
```

#### 2. 副标题/描述

```
文字内容：
Seamless cross-border logistics powered by Canada Post
partnership - from China collection to Canada delivery,
we handle every step with professional expertise and
reliable service.

样式规范：
- 字体：Inter Regular
- 字号：20px / 行高 32px
- 颜色：rgba(255, 255, 255, 0.85)
- 最大宽度：580px
- 间距：margin-bottom: 40px
- 动画：Fade In (1s, delay: 0.3s)
```

#### 3. CTA 按钮组

**主按钮 (Get Started)**：
```css
背景：linear-gradient(135deg, #FF9500 0%, #FFB340 100%)
文字：#FFFFFF, Inter SemiBold, 18px
圆角：8px
尺寸：180px × 56px
阴影：0 8px 16px -4px rgba(255, 149, 0, 0.4)
间距：margin-right: 16px

Hover效果：
- transform: translateY(-2px)
- box-shadow: 0 12px 20px -4px rgba(255, 149, 0, 0.5)
- transition: all 0.3s ease

动画：Fade In (1.2s, delay: 0.6s)
```

**次按钮 (Track Package)**：
```css
背景：transparent
边框：2px solid rgba(255, 255, 255, 0.3)
文字：#FFFFFF, Inter SemiBold, 18px
圆角：8px
尺寸：180px × 56px

Hover效果：
- border-color: rgba(255, 255, 255, 0.8)
- background: rgba(255, 255, 255, 0.1)
- transform: translateY(-2px)
- transition: all 0.3s ease

动画：Fade In (1.2s, delay: 0.6s)
```

#### 4. 货柜预订卡片 (左下角浮动)

```
位置：absolute, bottom: 60px, left: 0
尺寸：280px × 140px
背景：rgba(10, 38, 71, 0.95)
边框：1px solid rgba(255, 255, 255, 0.1)
圆角：16px
阴影：0 20px 40px -10px rgba(0, 0, 0, 0.3)
内边距：24px

内容：
┌─────────────────────────┐
│ 📦 UNLOADING            │
│                         │
│ Book Container          │
│ Unloading Today         │
│                         │
│ [Book Now →] 🟠         │
└─────────────────────────┘

样式：
- 图标：📦 Font size: 32px
- 标题："UNLOADING" - 12px, tracking: 2px, #FF9500
- 主文字：16px, #FFFFFF, Inter Medium
- 按钮：橙色文字 + 箭头

Hover效果：
- transform: translateY(-4px) scale(1.02)
- box-shadow: 0 24px 48px -12px rgba(0, 0, 0, 0.4)
- transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1)

3D效果：
- 添加 3D 货柜图标（SVG或PNG）
- 轻微旋转动画
```

---

### 右侧视觉区 (40%)

#### 1. 评价卡片 (右上角浮动)

```
位置：absolute, top: 80px, right: 80px
尺寸：240px × 160px
背景：rgba(255, 255, 255, 0.98)
圆角：20px
阴影：0 20px 40px -10px rgba(0, 0, 0, 0.15)
内边距：24px

内容：
┌──────────────────────────┐
│  👤👤👤  Leave a Review   │
│  ───────────────────────  │
│                           │
│         ⭐ 4.8           │
│                           │
│  Trusted by businesses    │
│  of all sizes.            │
│                           │
│  See why businesses       │
│  trust our services →     │
└──────────────────────────┘

样式：
- 用户头像：3个圆形头像，重叠排列
  - 尺寸：40px × 40px
  - 边框：2px solid #FFFFFF
  - margin-left: -12px (重叠效果)

- 评分：
  - 星星：⭐ Font size: 24px, color: #FFB340
  - 数字："4.8" - 48px, Inter Bold, #0A2647

- 文字：
  - 14px / 22px, Inter Regular, #4B5563

- 链接：
  - 14px, Inter Medium, #FF9500
  - Hover: text-decoration: underline

动画：
- Fade In + Slide Down (1s, delay: 0.8s)
- Hover: 轻微放大 scale(1.05)
```

#### 2. 背景图片处理

**EIE 货机照片**：
```
位置：absolute, right: 0, bottom: 0
尺寸：覆盖右侧 50%区域
图片：EIE 品牌货机起飞照片
处理：
- 蓝色叠加层：linear-gradient(90deg,
    rgba(10, 38, 71, 0.9) 0%,
    rgba(20, 66, 114, 0.6) 50%,
    rgba(32, 82, 149, 0.3) 100%
  )
- Blend mode: multiply
- 确保图片从右下角进入画面
- 飞机尾部完整显示

优化：
- 格式：WebP (备选：JPEG)
- 尺寸：1920px宽度
- 压缩：Quality 85
- 懒加载：loading="eager" (首屏)
```

#### 3. 物流追踪可视化 (右下角)

```
位置：absolute, bottom: 80px, right: 80px
尺寸：360px × 120px
背景：rgba(255, 255, 255, 0.95)
圆角：16px
阴影：0 12px 24px -8px rgba(0, 0, 0, 0.15)
内边距：24px

内容：
┌────────────────────────────────┐
│  CN SHG ════════════► US OAK  │
│    ●                       ○    │
│  May 3, 23:27      09:00 May 9 │
└────────────────────────────────┘

样式：
- 地点代码：16px, Inter Bold, #0A2647
- 连线：
  - 颜色：#FF9500 (橙色进度) + #E5E7EB (灰色背景)
  - 高度：4px
  - 动画：从左到右流动效果

- 节点：
  - 起点：实心圆，#FF9500, 12px
  - 终点：空心圆，#E5E7EB, 12px

- 时间：
  - 12px, Inter Regular, #6B7280

- 箭头：
  - Font size: 20px, color: #FF9500

动画：
- 连线流动：gradient移动动画 (2s, infinite)
- Fade In (1.2s, delay: 1s)
```

---

## 🎬 动画和交互效果

### 页面加载动画序列

```javascript
时间轴：
0.0s  - 背景渐变淡入
0.2s  - 货机照片淡入
0.4s  - Header导航淡入 + 下滑
0.6s  - 主标题淡入 + 上滑
0.9s  - 副标题淡入
1.2s  - 按钮组淡入
1.2s  - 评价卡片淡入 + 下滑
1.4s  - 物流追踪可视化淡入
1.4s  - 货柜预订卡片淡入 + 上浮
1.6s  - 开始连线流动动画

动画库：Framer Motion 或 GSAP
缓动函数：cubic-bezier(0.34, 1.56, 0.64, 1)
```

### Hover 交互效果

**主按钮**：
```javascript
onMouseEnter:
- transform: translateY(-2px)
- box-shadow: 增强
- background: 渐变位移

onMouseLeave:
- 回到初始状态
- transition: 0.3s ease
```

**卡片组件**：
```javascript
onMouseEnter:
- transform: translateY(-4px) scale(1.02)
- box-shadow: 增强
- 内部元素轻微动画

onMouseLeave:
- 回到初始状态
- transition: 0.4s cubic-bezier
```

### 滚动触发动画

```javascript
使用 Intersection Observer API

当用户向下滚动：
- Header 背景从半透明变为纯色
- 添加底部阴影
- Logo 略微缩小

触发点：scrollY > 100px
过渡时间：0.3s ease
```

---

## 📐 响应式设计方案

### 桌面端 (Desktop: ≥ 1280px)

```
Hero 区域：
- 高度：100vh (min-height: 800px)
- 左侧：60% 宽度
- 右侧：40% 宽度
- 内边距：80px

完全按照上述设计执行
```

### 平板端 (Tablet: 768px - 1279px)

```
Hero 区域：
- 高度：90vh (min-height: 700px)
- 左侧：55% 宽度
- 右侧：45% 宽度
- 内边距：40px

调整：
- 主标题：48px
- 副标题：18px
- 按钮：160px × 48px
- 评价卡片：缩小到 200px 宽
- 物流追踪：缩小到 300px 宽
```

### 移动端 (Mobile: < 768px)

```
Hero 区域：
- 高度：auto, min-height: 100vh
- 布局：单列，上下排列
- 内边距：24px

调整：
1. Header：
   - 汉堡菜单
   - Logo 居中或左对齐
   - CTA 按钮收起到菜单

2. 内容区：
   - 主标题：36px
   - 副标题：16px
   - 按钮：全宽，堆叠排列

3. 卡片：
   - 评价卡片：移到主文字下方
   - 物流追踪：移到按钮下方
   - 货柜预订卡片：移到页面底部

4. 背景图片：
   - 改为覆盖整个背景
   - 叠加层透明度提高到 0.85
   - 确保文字可读性
```

---

## 🎨 完整配色方案

### 主色调

```css
/* 深蓝色系 */
--navy-900: #0A2647;     /* 导航、标题 */
--navy-800: #144272;     /* 背景 */
--navy-700: #205295;     /* 次要元素 */
--navy-600: #2C74B3;     /* 装饰、图标 */

/* 橙色系 - 强调色 */
--orange-500: #FF9500;   /* 主CTA、重要元素 */
--orange-400: #FFB340;   /* Hover状态 */
--orange-600: #E68600;   /* Active状态 */

/* 中性色 */
--white: #FFFFFF;
--gray-50: #F9FAFB;
--gray-100: #F3F4F6;
--gray-200: #E5E7EB;
--gray-400: #9CA3AF;
--gray-600: #4B5563;
--gray-800: #1F2937;

/* 状态色 */
--success: #10B981;
--warning: #F59E0B;
--error: #EF4444;
--info: #3B82F6;
```

### 渐变系统

```css
/* Hero 背景渐变 */
--gradient-hero: linear-gradient(
  135deg,
  #0A2647 0%,
  #144272 50%,
  #205295 100%
);

/* 图片叠加渐变 */
--gradient-overlay: linear-gradient(
  90deg,
  rgba(10, 38, 71, 0.95) 0%,
  rgba(20, 66, 114, 0.85) 30%,
  rgba(32, 82, 149, 0.70) 60%,
  rgba(44, 116, 179, 0.50) 100%
);

/* 按钮渐变 */
--gradient-button: linear-gradient(
  135deg,
  #FF9500 0%,
  #FFB340 100%
);

/* 卡片渐变 */
--gradient-card: linear-gradient(
  180deg,
  rgba(255, 255, 255, 0.98) 0%,
  rgba(249, 250, 251, 0.95) 100%
);
```

### 阴影系统

```css
/* 卡片阴影 */
--shadow-card: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
--shadow-card-hover: 0 24px 48px -12px rgba(0, 0, 0, 0.20);

/* 按钮阴影 */
--shadow-button: 0 8px 16px -4px rgba(255, 149, 0, 0.4);
--shadow-button-hover: 0 12px 20px -4px rgba(255, 149, 0, 0.5);

/* 深色卡片阴影 */
--shadow-dark-card: 0 20px 40px -10px rgba(0, 0, 0, 0.3);
--shadow-dark-card-hover: 0 24px 48px -12px rgba(0, 0, 0, 0.4);

/* Header 阴影 */
--shadow-header: 0 2px 8px 0 rgba(0, 0, 0, 0.1);
```

---

## 📦 所需资源清单

### 图片资源

1. **EIE 货机照片**（已提供）：
   - 原图：用于Hero背景
   - 优化版本：WebP格式，1920px宽
   - 备选版本：JPEG格式，Quality 85

2. **EIE Logo**：
   - SVG格式（矢量）
   - PNG格式（透明背景，多尺寸）
   - 尺寸：原始 + 2x + 3x

3. **用户头像**（评价卡片）：
   - 3张圆形头像照片
   - 尺寸：80px × 80px (2x)
   - 格式：WebP 或 PNG

4. **3D 货柜图标**：
   - SVG 或 PNG
   - 尺寸：128px × 128px
   - 带轻微阴影和3D效果

### 图标库

```
推荐：Lucide React 或 Heroicons

需要的图标：
- ChevronDown (导航下拉)
- ArrowRight (按钮箭头)
- Package (货柜图标)
- Plane (航班图标)
- Star (评分星星)
- MapPin (地点标记)
- Clock (时间图标)
```

### 字体文件

```
Google Fonts: Inter

需要加载的字重：
- Inter Regular (400)
- Inter Medium (500)
- Inter SemiBold (600)
- Inter Bold (700)

优化：
- 使用 font-display: swap
- 预加载关键字体
- 只加载拉丁字符集（英文网站）
```

---

## 💻 技术实现方案

### 技术栈

```
框架：Next.js 14 (App Router)
语言：TypeScript
样式：Tailwind CSS
UI组件：Shadcn/ui + Radix UI
动画：Framer Motion
图标：Lucide React
```

### 组件结构

```
src/
├── components/
│   ├── layout/
│   │   ├── Header.tsx          # 顶部导航栏
│   │   ├── Navigation.tsx      # 导航菜单
│   │   └── MobileMenu.tsx      # 移动端菜单
│   │
│   ├── home/
│   │   ├── HeroSection.tsx     # Hero 主区域
│   │   ├── HeroContent.tsx     # 左侧内容
│   │   ├── HeroVisual.tsx      # 右侧视觉
│   │   ├── ReviewCard.tsx      # 评价卡片
│   │   ├── TrackingWidget.tsx  # 物流追踪
│   │   └── BookingCard.tsx     # 货柜预订卡片
│   │
│   └── ui/                     # Shadcn UI 组件
│       ├── button.tsx
│       ├── card.tsx
│       └── ...
│
├── app/
│   ├── page.tsx               # 首页
│   ├── layout.tsx             # 根布局
│   └── globals.css            # 全局样式
│
└── lib/
    ├── utils.ts               # 工具函数
    └── constants.ts           # 常量配置
```

### 核心代码示例

**HeroSection 组件**：
```typescript
"use client"

import { motion } from "framer-motion"
import Image from "next/image"
import HeroContent from "./HeroContent"
import HeroVisual from "./HeroVisual"

export default function HeroSection() {
  return (
    <section className="relative min-h-screen overflow-hidden">
      {/* 背景渐变 */}
      <div className="absolute inset-0 bg-gradient-hero" />

      {/* EIE 货机背景图 */}
      <div className="absolute right-0 bottom-0 w-1/2 h-full">
        <Image
          src="/images/eie-cargo-plane.webp"
          alt="EIE Cargo Plane"
          fill
          priority
          className="object-cover object-left-bottom"
        />
        <div className="absolute inset-0 bg-gradient-overlay" />
      </div>

      {/* 主内容 */}
      <div className="relative container mx-auto px-20 py-32">
        <div className="grid grid-cols-[60%_40%] gap-12 items-center">
          {/* 左侧内容 */}
          <HeroContent />

          {/* 右侧视觉元素 */}
          <HeroVisual />
        </div>
      </div>
    </section>
  )
}
```

---

## 🚀 性能优化

### 图片优化

```typescript
// Next.js Image 组件配置
<Image
  src="/images/eie-cargo-plane.webp"
  alt="EIE Cargo Plane"
  fill
  priority              // 首屏图片优先加载
  quality={85}          // 压缩质量
  placeholder="blur"    // 模糊占位符
  blurDataURL="..."     // Base64占位图
/>
```

### 代码分割

```typescript
// 懒加载非关键组件
const ReviewCard = dynamic(() => import("./ReviewCard"), {
  loading: () => <Skeleton className="w-60 h-40" />
})

const TrackingWidget = dynamic(() => import("./TrackingWidget"), {
  ssr: false  // 客户端渲染
})
```

### CSS 优化

```css
/* 关键CSS内联 */
/* 使用 Tailwind CSS 的 JIT 模式 */
/* 移除未使用的样式 */

/* 字体优化 */
@font-face {
  font-family: 'Inter';
  font-style: normal;
  font-weight: 400;
  font-display: swap;
  src: url('/fonts/inter-v12-latin-400.woff2') format('woff2');
}
```

### 动画性能

```typescript
// 使用 transform 和 opacity（GPU加速）
// 避免触发重排（reflow）

const fadeInUp = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  transition: { duration: 0.6, ease: "easeOut" }
}

// 使用 will-change 提示浏览器
<motion.div
  style={{ willChange: "transform, opacity" }}
  variants={fadeInUp}
>
```

---

## ✅ 开发检查清单

### 设计阶段
- [ ] 确认所有视觉设计稿
- [ ] 准备所有图片资源
- [ ] 确认配色方案
- [ ] 确认字体选择
- [ ] 准备图标资源

### 开发阶段
- [ ] 搭建 Next.js 项目
- [ ] 配置 Tailwind CSS
- [ ] 安装必要依赖
- [ ] 创建组件结构
- [ ] 实现 Header 导航
- [ ] 实现 Hero 左侧内容
- [ ] 实现 Hero 右侧视觉
- [ ] 添加动画效果
- [ ] 响应式适配
- [ ] 性能优化

### 测试阶段
- [ ] 桌面端测试（Chrome/Safari/Firefox）
- [ ] 平板端测试（iPad）
- [ ] 移动端测试（iPhone/Android）
- [ ] 性能测试（Lighthouse）
- [ ] 可访问性测试（WCAG）
- [ ] 跨浏览器测试

### 部署阶段
- [ ] 图片压缩优化
- [ ] 代码打包优化
- [ ] SEO 元标签设置
- [ ] 部署到 Vercel
- [ ] 配置域名
- [ ] 配置 Analytics

---

## 📊 预期效果指标

### 性能指标

```
Lighthouse Score 目标：
- Performance: > 95
- Accessibility: > 95
- Best Practices: > 95
- SEO: > 95

Core Web Vitals 目标：
- LCP (Largest Contentful Paint): < 2.5s
- FID (First Input Delay): < 100ms
- CLS (Cumulative Layout Shift): < 0.1
```

### 用户体验指标

```
- 首屏加载时间: < 2s
- 页面完全加载: < 3s
- 动画流畅度: 60 FPS
- 移动端体验: 流畅无卡顿
```

---

## 🎯 下一步行动

1. **立即开始**：
   - 准备 EIE Logo SVG 文件
   - 优化货机照片（WebP格式）
   - 准备用户头像照片

2. **本周完成**：
   - 搭建 Next.js 项目
   - 实现 Hero 区域
   - 完成响应式适配

3. **测试部署**：
   - 跨设备测试
   - 性能优化
   - 部署上线

---

**文档版本**: v1.0
**创建日期**: 2025-11-03
**最后更新**: 2025-11-03
